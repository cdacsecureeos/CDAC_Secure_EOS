[Match]
Name=end1  # Your network interface

[Network]
DHCP=yes
IPv6AcceptRA=no  # Disable IPv6 Router Advertisements (if not needed)

[DHCP]
UseMTU=false
UseDomains=false
RouteMetric=10
TimeoutSec=3  # Reduce DHCP timeout (default is 30s)
